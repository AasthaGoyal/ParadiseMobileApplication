<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.assignment2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".FeaturesActivity" />
        <activity android:name=".AccessCareerDataActivity" />
        <activity android:name=".AccessUserDataActivity" />
        <activity android:name=".AccessMenuDataActivity" />
        <activity android:name=".SearchUserActivity" />
        <activity android:name=".ContactsActivity" />
        <activity android:name=".SendMessageActivity" />
        <activity android:name=".SearchMenu" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".GoogleMapsActivity"
            android:label="@string/title_activity_google_maps" />
        <activity android:name=".ImageSliderActivity" />
        <activity android:name=".AboutAisActivity" />
        <activity android:name=".AboutItpActivity" />
        <activity
            android:name=".AboutUsActivity"
            android:label="@string/title_activity_about_us"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AboutDeveloperActivity" />
        <activity android:name=".MenuDataActivity" />
        <activity android:name=".LoadMenuDataActivity" />
        <activity android:name=".SearchCareerActivity" />
        <activity android:name=".LoadCareerDataActivity" />
        <activity android:name=".LoadUserDataActivity" />
        <activity android:name=".LoadSpecialsDataActivity"></activity>
    </application>

</manifest>